server:
    port: 8070

spring:
    application:
        name: mycart
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        username: root
        password: root
        url: jdbc:mysql://localhost:3306/graphqldb
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true